set shell := ["powershell.exe", "-c"]

default:
  just --list

compile:
  cargo build

compileopt:
  cargo build --release

crun: compile run

run *ARGS:
  .\target\debug\exemple {{ARGS}}

test:
  cargo test

bench *ARGS:
  hyperfine ".\target\debug\exemple  {{ARGS}}"

benchopt *ARGS:
  hyperfine --warmup 3 ".\target\release\exemple  {{ARGS}}"

benchmem *ARGS:
  ..\Measure-Exec.ps1 ".\target\release\exemple" -Arguments "{{ARGS}}"

