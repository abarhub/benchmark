set shell := ["powershell.exe", "-c"]

default:
  just --list


run *ARGS:
  uv run main.py {{ARGS}}

test:
  uv run pytest -v

bench *ARGS:
  hyperfine --warmup 3 "uv run main.py {{ARGS}}"

benchmem *ARGS:
  ..\Measure-Exec.ps1 "uv" -Arguments "run main.py {{ARGS}}"
